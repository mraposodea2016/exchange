import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    subScreenNav: {
        flex: 1,
        width: "70%",
    },
    subScreenNavButton: {
        margin: 10
    }
});

export default styles;
